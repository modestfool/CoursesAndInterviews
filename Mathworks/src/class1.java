import java.sql.Struct;
import java.util.ArrayList;

/**
 * 
 */

/**
 * class1.java 
 * @author Basava R.Kanaparthi(basava.08@gmail.com)
 * Created on Feb 12, 2016
 */
public class class1 {

	int n1,d1,n2,d2;
	
	n1 = scanf("%d",)
		
		Struct n1;
		n1.equals(obj)
		
		printf( "%d%c%d\n",num,"/",den);
		struct fraction *ans = malloc(sizeof(struct fraction));
		num1->num = 10;
		add(num1,num2,ans);
		
		Fraction num1 = new Fraction();
		num1.setNum(10);
		num2.setNum(15);
		num2.setDen
		
}

class Fraction{
	private int num;
	private int den;
	
	
	
	public Fraction (ArrayList<Fraction> numbers)
	{
	
		Fraction ans = new Fraction();
		int maxIndex = numbers.length - 1;
		for (Fraction num in numbers)
		{
			ans.setNum(ans.getNum*num.getDen + ans.Num*num.den);
			ans.setDen(num.getDen*ans.getDen);
		}
		return ans;
	}
}

struct fraction{
	int num;
	int den;
};

struct fraction add(struct fraction* num1, struct fraction* num2, struct fraction* ans)
{
	ans->num = (num1->num)
	(*ans).num = num1.num*num2.den + num2.num*num1.den;
	ans.den = num1.den*num2.den;
	
	return ans;
}